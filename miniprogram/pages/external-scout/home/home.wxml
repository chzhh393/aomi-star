<!--pages/external-scout/home/home.wxml-->
<!--星探工作台 - 街头运动风格-->
<view class="container">

  <!-- 顶部横幅 -->
  <view class="header-banner">
    <view class="banner-slash"></view>
    <text class="banner-text">SCOUT</text>
    <text class="banner-subtitle">星探工作台</text>
  </view>

  <!-- 欢迎卡片 -->
  <view class="welcome-card">
    <view class="welcome-icon">🔍</view>
    <view class="welcome-content">
      <view class="welcome-title">你好，{{user.profile.nickname || user.profile.name}}</view>
      <view class="welcome-desc">星探 | 推荐ID: {{user.id}}</view>
    </view>
  </view>

  <!-- 统计数据 -->
  <view class="stats-block">
    <view class="block-label">
      <view class="label-slash"></view>
      <text class="label-text">MY STATS</text>
    </view>

    <view class="stats-grid">
      <view class="stat-item">
        <view class="stat-value">{{stats.totalReferred}}</view>
        <view class="stat-label">推荐总数</view>
      </view>

      <view class="stat-item">
        <view class="stat-value">{{stats.pendingReview}}</view>
        <view class="stat-label">审核中</view>
      </view>

      <view class="stat-item">
        <view class="stat-value">{{stats.signed}}</view>
        <view class="stat-label">已签约</view>
      </view>

      <view class="stat-item highlight">
        <view class="stat-value">¥{{stats.totalReward}}</view>
        <view class="stat-label">累计奖励</view>
      </view>
    </view>
  </view>

  <!-- 快捷功能 -->
  <view class="actions-block">
    <view class="block-label">
      <view class="label-slash"></view>
      <text class="label-text">QUICK ACCESS</text>
    </view>

    <view class="actions-list">
      <block wx:for="{{quickActions}}" wx:key="id">
        <view
          class="action-item"
          data-id="{{item.id}}"
          bindtap="onActionTap"
        >
          <view class="item-accent" style="background: {{item.color}};"></view>

          <view class="item-icon">
            <text class="icon-text">{{item.icon}}</text>
          </view>

          <view class="item-content">
            <view class="item-title">{{item.title}}</view>
            <view class="item-desc">{{item.desc}}</view>
          </view>

          <view class="item-arrow">→</view>
        </view>
      </block>
    </view>
  </view>

  <!-- 最近推荐 -->
  <view class="recent-block" wx:if="{{recentCandidates.length > 0}}">
    <view class="block-label">
      <view class="label-slash"></view>
      <text class="label-text">RECENT</text>
    </view>

    <view class="candidate-list">
      <block wx:for="{{recentCandidates}}" wx:key="id">
        <view
          class="candidate-item"
          data-id="{{item.id}}"
          bindtap="viewCandidateDetail"
        >
          <view class="candidate-name">{{item.name}}</view>
          <view class="candidate-status status-{{item.status}}">
            {{item.status === 'pending' ? '审核中' :
              item.status === 'interview' ? '面试中' :
              item.status === 'signed' ? '已签约' :
              item.status === 'active' ? '活跃' : '其他'}}
          </view>
        </view>
      </block>
    </view>
  </view>

  <!-- 温馨提示 -->
  <view class="tips-block">
    <view class="tip-item">
      <text class="tip-icon">💰</text>
      <text class="tip-text">每成功推荐一位主播签约，可获得500元奖励</text>
    </view>
    <view class="tip-item">
      <text class="tip-icon">📤</text>
      <text class="tip-text">转发小程序给潜在主播，使用您的专属推荐码</text>
    </view>
  </view>

</view>
